// 1. string[] 타입 주석을 사용하여 문자열 배열을 선언합니다.
let projectNames: string[] = ["팀 A 프로젝트", "팀 B 프로젝트", "팀 C 프로젝트"];

// 2. [number, string, boolean] 타입 주석을 사용하여 튜플을 선언합니다.
let userRecord: [number, string, boolean] = [202401, "박지민", true];

// 3. projectNames 배열의 두 번째 요소 (인덱스 1)를 할당합니다.
let secondProject: string = projectNames[1]; // 값: "팀 B 프로젝트"

// 4. userRecord 튜플의 사용자 이름 (인덱스 1)을 할당합니다.
let recordName: string = userRecord[1]; // 값: "박지민"

// 5. 결과 출력
console.log(`두 번째 프로젝트: ${secondProject}`);
console.log(`기록된 사용자 이름: ${recordName}`);

// 🚨 오류 예시 (주석 처리):
// userRecord.push(123); // 튜플은 push를 허용하지만, TypeScript는 이를 권장하지 않습니다.
// userRecord[0] = "error"; // 컴파일 오류: 첫 번째 요소는 number 타입이어야 합니다.
// 💡 해설: 배열(요소 타입만 일치)과 튜플(순서와 타입까지 일치)의 차이를 이해하는 것이 중요합니다.